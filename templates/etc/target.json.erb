{
<% @compute_hosts.each_index do |host_index| -%>
<% if host_index == @compute_hosts.size - 1 -%>
        "<%= @compute_hosts.at(host_index) %>":
        {
                "ip": "<%= @ipmi_ips.at(host_index) %>",
                "username": "<%= @ipmi_usernames.at(host_index) %>",
                "password": "<%= @ipmi_passwords.at(host_index) %>"
        }
<% else -%>
        "<%= @compute_hosts.at(host_index) %>":
        {
                "ip": "<%= @ipmi_ips.at(host_index) %>",
                "username": "<%= @ipmi_usernames.at(host_index) %>",
                "password": "<%= @ipmi_passwords.at(host_index) %>"
        },
<% end -%>
<% end -%>
}
